<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="0dp">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:text="@string/no_legs"
            android:layout_centerHorizontal="true"
            android:textColor="@android:color/black"
            android:visibility="gone"
            android:id="@+id/no_legs"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/itinerary_header"
        android:layout_margin="@dimen/_16sdp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/edit_leg_container"
            android:layout_margin="0dp"
            android:visibility="gone"/>

        <Button
            android:id="@+id/add_leg_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/major_text"
            android:textAllCaps="false"
            android:background="@android:color/darker_gray"
            android:layout_marginTop="@dimen/text_margins"
            android:visibility="gone"
            android:layout_below="@id/edit_leg_container"
            android:layout_centerHorizontal="true"
            />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/add_leg_container"
            android:layout_margin="0dp"
            android:layout_below="@id/add_leg_button"
            android:visibility="gone"/>

    </RelativeLayout>


    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/leg_list_container"
        android:layout_margin="0dp"
        android:layout_below="@id/itinerary_header"
        />

    </RelativeLayout>

</ScrollView>